#!/bin/bash

set -eo pipefail

eval "$(cargo-cross-env)"

cargo install \
    --root /usr/local \
    cargo-cache \
    cargo-edit \
    cargo-udeps \
    sccache

cargo install \
    --root /usr/local \
    --no-default-features \
    --features postgres \
    diesel_cli@1.4.1
